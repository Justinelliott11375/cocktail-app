<!DOCTYPE html>
<html>
    <head>
        <% include ../static/partials/head.ejs %>
    </head>
    <body>
        <% include ../static/partials/navbar.ejs %>

        <main class="container">
            <% include ../static/partials/messages.ejs %>
            
            <h1><%= list.title %></h1>
                <% if(currentUser) { %>
                    <a href="/lists/<%=list.id%>/posts/new" class="btn btn-success">New Recipe</a>
                        <% if(currentUser.isAdmin()) { %>
                            <a href="/lists/<%= list.id %>/edit" class="btn btn-warning">Edit</a>
                            <form style="display:initial" action="/lists/<%= list.id %>/destroy" method="post">
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                        <% } %>
                <% } %>
            <p><%= list.description %></p>
            <hr>

            <h1>Recipes</h1>

            <ul class="list-group">
                <% list.posts.forEach((post) => { %>
                    <li class="list-group-item">
                        <a href="/lists/<%= list.id %>/cards/<%= card.id %>"> <%= card.title %> </a>
                    </li>
                <% }) %>
            </ul>
        </main>

        <% include ../static/partials/baseScripts.ejs %>

    </body>
</html>